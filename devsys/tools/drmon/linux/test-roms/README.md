# test-roms

`drmon-test.md` — minimal 1 KB Genesis/68000 ROM: reset vectors + infinite `BRA *` at 0x200.
Generated by the inline Python script in `test_bridge.sh` history.  Safe to commit (no
third-party code).

For SNES tests (`task test-bridge SYS=snes`), place any valid SNES ROM (`.sfc` or `.smc`)
here.  SNES ROMs are copyrighted; do not commit them.
