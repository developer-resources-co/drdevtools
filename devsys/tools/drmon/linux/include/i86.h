// Shim <i86.h> — Watcom DOS intrinsics. Only referenced from __WATCOMC__ paths,
// which we don't build; empty so any stray include is harmless.
#ifndef DRMON_SHIM_I86_H
#define DRMON_SHIM_I86_H
#endif
