// Shim <phapi.h> — PharLap DOS-extender API. Only referenced from DOSX286 paths,
// which we don't build; empty so any stray include is harmless.
#ifndef DRMON_SHIM_PHAPI_H
#define DRMON_SHIM_PHAPI_H
#endif
