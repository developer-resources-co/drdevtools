# Phase 3 — drmon-dap: DAP front end

**Date:** 2026-06-12  
**Status:** **COMPLETE.** Tiers 1–3 done (2026-06-12); **live-MAME V3–V6 automated + PASS (2026-06-18,
`task test-dap`)**. **VS Code GUI confirmed live (2026-06-19):** extension loads + attaches; a source
breakpoint on `a16local.c:17` **fires**, the CPU **halts** (PC=`$8074`), and VS Code **highlights line
17** with the CALL STACK mapped to `a16local.c:17` (screenshots in Tier-3 v6). The GUI exercise found +
fixed two adapter bugs (#5 `configurationDone` "go", #6 `stackTrace` source mapping; drdevtools
`545723a`), also covered by `task test-dap` → `phasec` 8/8. VS Code needs the `vscode-drmon/` extension
via `--extensionDevelopmentPath`.  
**Commit:** 71dbba7

---

## Summary

Two parallel binaries: `drmon-dap-snes` and `drmon-dap-gen`, each a standalone C++17 DAP adapter
that speaks to VS Code / nvim-dap over stdin/stdout and to MAME over the existing TCP bridge on
`:41816`.  `snesmon`/`genmon` stay functional indefinitely — "two binaries in parallel", not a TUI
deprecation.

```
VS Code / nvim-dap  ←— stdin/stdout DAP ——→  drmon-dap  ←— TCP :41816 ——→  mame_bridge.lua → MAME
```

---

## Architecture

### Why a new binary, not a TUI refactor

The TUI modules (`break.cpp`, `symbol.cpp`, `expr.cpp`, `memory.cpp`, `reg.cpp`,
all of screen/layer/window/gadget) are 40–60% entangled with the `_object`/`_layer`
event loop via `moninc.hpp`.  Surgically separating them would be high-risk and low-return.

**New `drmon-dap` binary** compiles only the clean modules:
- `linux/dap/backend.cpp` — fresh MAME TCP client (no sliomame.cpp dependency;
  avoids the reg.cpp / control.cpp / moninc.hpp chain entirely)
- `linux/dap/session.cpp` — cppdap DAP request handlers
- `linux/dap/breakpoints.cpp` — address breakpoint tracking
- `linux/dap/registers.hpp` — per-system register name/mask tables

cppdap fetched via CMake `FetchContent` at tag `dap-1.58.0-a`.

### Step engine

`backend.cpp` includes a complete 65816 opcode-size table (256 entries).
`snesNextPC()` reads the opcode at PC, adjusts size for M/X flag variants
(immediate operands), handles branches (conditional + unconditional) and
long jumps by computing BOTH possible next addresses, and sends `S <next> [<alt>]`
to the bridge.  Correct for all common SNES code paths.

---

## Tier 1 DAP surface (complete)

| Request | Implementation |
|---------|---------------|
| `initialize` | Return capabilities; send `InitializedEvent` |
| `attach` | `MameBackend::connect()` (host/port from CLI) |
| `configurationDone` | Start poll thread (50 ms cycle) |
| `setInstructionBreakpoints` | `B+`/`B-` bridge commands; `BreakpointTable::sync()` |
| `continue` | `C` command |
| `pause` | `H` command |
| `next` / `stepIn` | `snesNextPC()` → `S <addr>` |
| `threads` | Static: `[{id:1, name:"maincpu"}]` |
| `stackTrace` | One frame at current PCL |
| `scopes` | One scope: "Registers" (ref=1) |
| `variables` | All registers from `G` response; each sets `memoryReference` → VS Code Memory Inspector icon |
| `readMemory` | `R` command + base64 |
| `evaluate` | `$addr` → byte read; register name → value |
| `disconnect` / `terminate` | Stop poll thread, `BYE` |

Poll thread: fires `StoppedEvent{reason:"breakpoint"|reason}` on running→stopped transition.

---

## Tier 2 (complete 2026-06-12)

- `disassemble` request: `dis816_dap.cpp`/`dis68000_dap.cpp` wrappers + `disasm_preamble.hpp`/
  `disasm_stubs.cpp` break the `moninc.hpp` dependency; `procMode` derived from live FLAGS.
- `supportsDisassembleRequest = true` capability.
- [plan](2026-06-12-phase-3-drmon-dap-tier-2-disassembly-view.md)

## Tier 3 (complete 2026-06-12)

- Fresh `SymbolTable` in `linux/dap/symbols.hpp/.cpp`: binary `.sld` loader (source line
  records) + Sierra COFF loader (label→addr); no TUI dependency.
- `--symbols <path>` CLI flag; `disassemble` label annotation; `evaluate` symbol lookup;
  `setBreakpoints` source-line resolution; `loadedSources` handler.
- [plan](2026-06-12-phase-3-drmon-dap-tier-3-symbol-loading.md)

---

## Build

```cmake
# CMakeLists.txt additions:
FetchContent_Declare(cppdap GIT_REPOSITORY ... GIT_TAG dap-1.58.0-a)
drmon_dap_target(drmon-dap-snes DRMON_DAP_SNES)
drmon_dap_target(drmon-dap-gen  DRMON_DAP_GEN)
```

Dockerfile: added `git` package for FetchContent clone.

---

## Verification

1. **Build** — `task build` produces `drmon-dap-snes` and `drmon-dap-gen` alongside
   `snesmon`/`genmon` with no errors.

   ```
   [10/12] Linking CXX static library _deps/cppdap-build/libcppdap.a
   [11/12] Linking CXX executable drmon-dap-gen
   [12/12] Linking CXX executable drmon-dap-snes
   ```
   **PASS** (commit 71dbba7)

2. **Protocol smoke** — `printf "Content-Length: 83\r\n\r\n..." | ./drmon-dap-snes`
   returns well-formed `InitializedEvent` + `InitializeResponse`.

   ```
   Content-Length: 56

   {"body":{},"event":"initialized","seq":1,"type":"event"}Content-Length: 201

   {"body":{"supportsConfigurationDoneRequest":true,"supportsInstructionBreakpoints":true,
   "supportsReadMemoryRequest":true},"command":"initialize","request_seq":1,"seq":2,
   "success":true,"type":"response"}
   ```
   **PASS** (commit 71dbba7)

**Live-MAME items 3–6: automated + PASS (2026-06-18).** New harness `linux/test_dap.sh` +
`linux/dap/test_dap.py` (run via `task test-dap`): brings up headless MAME + the SNES Lua bridge on
the host and drives `drmon-dap-snes` over DAP stdio inside the build container (`--network=host`), then
cross-checks against a direct bridge connection. **3 consecutive runs, 11/11 each.** Reference ROM:
`test-roms/drmon-test.sfc` (NOP sled $8000–$FFBB looping via JML $008000).

```
  PASS: initialize succeeds
  PASS: V3 attach succeeds
  PASS: V3 connected: instruction breakpoint accepted
  PASS: V3 breakpoint reports its address
  PASS: V4 StoppedEvent(reason=breakpoint) fires
  PASS: V4 PC at/just past breakpoint 0x8100 (Tier-1 bridge holds a few insns past)
  PASS: V5 Registers scope has all 11 SNES registers
  PASS: V5 PCL at/just past breakpoint 0x8100 (matches stackTrace PC)
  PASS: V6 readMemory($8000,16) == NOP sled (0xEA×16)
  PASS: V5 cross-check: bridge PCL == drmon PCL
  PASS: V6 cross-check: bridge R($8000,16) == drmon readMemory
Results: 11 passed, 0 failed
```

3. **Connected session** — attach succeeds; an instruction breakpoint round-trips to MAME (proves the
   adapter is live, not just echoing). **PASS** (test_dap.py V3).

4. **Breakpoint fires** — `setInstructionBreakpoints $8100` → `continue` → `StoppedEvent(breakpoint)`;
   `stackTrace` PC lands at/just past $8100. **PASS** (test_dap.py V4). NB: the Tier-1 Lua bridge
   (`-debugger none`) pseudo-holds a NOP or two past the bp (marker-detected stop on the next periodic
   tick); the Tier-2 C++ gdbstub freezes exactly. PC == $8101 here, inside the sled — expected.

5. **Registers** — `variables` (Registers scope) returns all 11 SNES regs; PCL matches the stop PC and
   matches a direct bridge `G` read. **PASS** (test_dap.py V5 + cross-check).

6. **Memory** — `readMemory $8000` == `0xEA×16` (NOP sled) and matches a direct bridge `R` read.
   **PASS** (test_dap.py V6 + cross-check). (Probed the ROM window rather than `$7E0000` WRAM — the
   sled ROM is the deterministic known-content region.)

   Harness lessons (folded into test_dap.sh / test_dap.py): MAME needs **`-skip_gameinfo`** headless
   (else it stalls on the disclaimer, emulated time never advances, the autoboot bridge never binds);
   run **throttled** (`-nothrottle` starves the bridge's per-frame socket handling → attach-time
   connect hangs); and DAP requests must always carry an **`arguments`** field (cppdap silently drops
   `attach`/`configurationDone` without it).

7. **Disassembly** — the `disassemble` *request* is exercised offline (Tier 2 plan v1–3, 5 PASS) and
   live registers/memory above confirm the bridge path; the **VS Code disassembly *pane*** (GUI) is a
   manual confirmation of an already-working request — see Tier 2 plan v4. **(Manual VS Code step.)**

8. **Regression** — `task smoke SYS=snes` still passes; TUI binaries untouched.

   ```
   PASS: opened windows via Alt-keys (M-e M-k M-r M-m M-w M-b M-s M-i M-o M-a M-y)
   + typed into Expression — no SIGSEGV
   ```
   **PASS** (commit 71dbba7)
